Verification probe for the new-project-branch-hygiene workflow.

This file exists only on a throwaway branch whose pull request is closed
without merging, to exercise the automatic head-branch deletion. It never
reaches main.
