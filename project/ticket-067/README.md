# Ticket 067: Release monag 0.3.2 to PyPI via goal -a

- **ID**: ticket-067
- **Owner**: human:founder
- **Status**: IN_PROGRESS
- **Workflow state**: EDIT
- **Created**: 2026-09-18
- **Authorization**: SESSION_EXECUTION_AUTHORIZATION (user requested PyPI release of monag via goal -a)

## Goal and scope

Release `monag` 0.3.2 to PyPI with updated versioning, changelog, and enabled publishing configuration.

## Acceptance criteria

- [x] AC-01: `VERSION`, `pyproject.toml`, and `src/monag/__init__.py` bumped to 0.3.2.
- [x] AC-02: `CHANGELOG.md` updated with 0.3.2 release notes.
- [x] AC-03: `goal.yaml` publishing enabled and configured for PyPI release.
- [x] AC-04: PyPI artifact built and published successfully.

## Tracking boundary

This directory contains the minimal reviewed intent. Optional participant prose
and raw command logs are not required delivery output.
